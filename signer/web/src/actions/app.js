import { createAction } from 'redux-actions';

export const updateIsConnected = createAction('update isConnected');
export const updateIsNodeRunning = createAction('update isNodeRunning');
export const updateProxy = createAction('update proxy');
export const updateUrl = createAction('update url');
