import { createAction } from 'redux-actions';

export const error = createAction('error dapps');
export const gotoDapp = createAction('goto dapp');
export const updateDapps = createAction('update dapps');
