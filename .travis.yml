language: rust
rust:
  - stable
  - beta
  - nightly

matrix:
  fast_finish: true
  allow_failures:
    - rust: nightly
  env:
    - PROJECT=parity-dapps-builtins

script:
  - cargo build -p $PROJECT
  - cargo test --release -p $PROJECT

